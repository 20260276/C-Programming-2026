#include <stdio.h>
int main(void){
	int korean;
	int math;
	double average;
	
	scanf("%d",&korean);
	scanf("%d",&math);
	average = (korean + math) / 2.0;
	printf("%d + %d = %d\n", korean, math, korean + math);
	printf("average: %.lf\n",average);
	return 0;
}
